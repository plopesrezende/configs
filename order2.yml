server:
  port: 9093

info:
  component: Client Order 2
  
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:9091/eureka/
  instance:
    hostname: localhost

ribbon:
  eureka:
    enabled: false